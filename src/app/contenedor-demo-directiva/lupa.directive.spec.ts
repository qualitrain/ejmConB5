import { LupaDirective } from './lupa.directive';

describe('LupaDirective', () => {
  it('should create an instance', () => {
    const directive = new LupaDirective();
    expect(directive).toBeTruthy();
  });
});
