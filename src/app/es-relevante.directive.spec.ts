import { EsRelevanteDirective } from './es-relevante.directive';

describe('EsRelevanteDirective', () => {
  it('should create an instance', () => {
    const directive = new EsRelevanteDirective();
    expect(directive).toBeTruthy();
  });
});
