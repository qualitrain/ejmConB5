import { PlantillaNgDirective } from './plantilla-ng.directive';

describe('PlantillaNgDirective', () => {
  it('should create an instance', () => {
    const directive = new PlantillaNgDirective();
    expect(directive).toBeTruthy();
  });
});
